export class Calc {
    add (a: number, b: number){
        return a + b;
    }

    multiply(a: number, b: number){
        return a * b;
    }

    square(a: number){
        return Math.pow(a,2);
    }
}